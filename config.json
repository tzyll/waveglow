{
    "comments": "raw audio, a seg audio as a sample, no condition",

    "train_config": {
        "fp16_run": true,
        "output_directory": "checkpoints_raw-audio_seg4000",
        "epochs": 100000,
        "learning_rate": 1e-4,
        "sigma": 1.0,
        "iters_per_checkpoint": 200,
        "batch_size": 64,
        "seed": 1234,
        "checkpoint_path": "",
        "with_tensorboard": true
    },
    "data_config": {
        "training_files": "data_timit/train/wav.scp",
        "segment_length": 4000,
        "sampling_rate": 16000,
        "filter_length": 1024,
        "hop_length": 256,
        "win_length": 1024,
        "mel_fmin": 0.0,
        "mel_fmax": 8000.0
    },
    "dist_config": {
        "dist_backend": "nccl",
        "dist_url": "tcp://localhost:54321"
    },

    "waveglow_config": {
        "n_mel_channels": 80,
        "n_flows": 12,
        "n_group": 4000,
        "n_early_every": 4,
        "n_early_size": 1000,
        "WN_config": {
            "n_layers": 4,
            "n_channels": 128,
            "kernel_size": 3
        }
    }
}
